import logo from '../assets/rootbase-logo.png'
const SideNav = () => {

    return (
        <div id="side-nav">
            <img id="side-nav-logo" src={logo} />
        </div>
    )
}

export default SideNav